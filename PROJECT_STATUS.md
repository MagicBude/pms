# PMS 项目状态

> 这是项目的唯一当前状态页。每次开始工作先读本页，每次结束工作更新本页。

- 最后更新：2026-08-23
- 当前阶段：Phase 1（工程基础）
- 当前结论：F-001 至 F-010 已关闭；F-011 首次远端烟雾失败，Linux 连续重启端口预检修复待验证
- 下一里程碑：提交 F-011 CI 修复并完成 F-012 Phase 1 最终退出审查

## 已完成

- [x] 将旧项目完整迁入 `.internal/legacy-pms/`，并由 Git 忽略。
- [x] 建立产品、架构、部署、开发、迁移、安全和工程规范目录。
- [x] 建立代码、注释、命名、文档、测试、日志、安全和 Git 规范。
- [x] 建立项目蓝图和三阶段部署方向：本机、内网、云端。
- [x] 建立项目状态、路线图、工作日志、变更记录和 AI 交接机制。
- [x] 完成第一轮旧系统静态盘点：选择 55 个关键工作簿，其中 39 个含 VBA，共提取 317 个模块、1,309 个过程，错误 0 个。
- [x] 建立旧功能清单、业务规则、新功能目录和迁移追踪矩阵初稿。
- [x] 首次提交 `e0d3ef1` 已由用户推送到 `origin/main`，本地与远程同步。
- [x] 完成 P0 范围分层，并形成 `SLICE-001` 角色、流程、状态、字段、权限和验收提案。
- [x] 用户接受 P0 分层以及 `D-S001` 至 `D-S012`；`SLICE-001` 正式确定。
- [x] 建立 `SLICE-001` 业务术语、角色权限和核心状态机基线。
- [x] 形成系统上下文、模块边界以及技术栈、数据库/租户、附件、部署四份关键 ADR 提案。
- [x] 用户确认继续下一步，`ADR-0001` 至 `ADR-0004` 转为“已接受”。
- [x] 按用户提供的参考规范完善 PMS 提交格式、scope、正文、模板和检查要求。
- [x] 形成 F-001 至 F-009 工程骨架与质量工具实施计划草案。
- [x] 用户确认继续下一步，工程骨架与质量工具实施计划转为“已接受”。
- [x] 将 14 条候选验收摘要细化为 45 条带稳定 ID 和测试层级的 Given/When/Then 案例。
- [x] 执行 Phase 0 退出审查，确认当前仅剩验收案例接受门槛。
- [x] 用户接受 `SLICE-001` 验收案例，Phase 0 退出审查通过并进入 Phase 1。
- [x] 完成 F-001：安装 uv 与 CPython 3.14.7，建立 Python 包、依赖分组、跨平台锁文件和依赖评审。
- [x] 完成 F-002：建立最小 Django ASGI、URL、四套配置档案和启动安全测试。
- [x] 完成 F-003：建立 Ruff、mypy、pytest、80% 覆盖率、导入边界和 GitHub Actions 门槛。
- [x] 完成 F-004：自有 UUIDv7 用户、认证与会话基础，以及 SQLite/PostgreSQL 18 首次迁移验证。
- [x] 完成 F-005：tenant、membership、可信 `TenantContext` 和 SQLite/PostgreSQL 18 验证。
- [x] 完成 F-006：稳定权限、默认角色矩阵、对象范围、追加式审计和 SQLite/PostgreSQL 18 验证。
- [x] 完成 F-007：健康探针、request ID、结构化日志、安全错误响应和 PostgreSQL 18 验证。
- [x] 完成 F-008：附件元数据、存储端口、本地原子写入、失败补偿、一致性对账和 PostgreSQL 18 验证。
- [x] 完成 F-009：显式初始化默认租户、管理员、权限角色矩阵，并完成 SQLite/PostgreSQL 18 空库启动烟雾验证。
- [x] 在另一空目录完成 Python、依赖、质量链、迁移、初始化和 Uvicorn 启动复验，复验仓库保持干净。
- [x] 执行 Phase 1 退出审查；确认工程计划门槛通过，但蓝图仍有本机备份恢复和启动器两个缺口。
- [x] 建立 F-010 本机候选：SQLite 在线快照、附件一致性复制、版本化清单、离线校验和不覆盖原数据的空目录恢复。
- [x] GitHub Actions 运行 `32648596251` 通过本机备份恢复、恢复目录启动和 PostgreSQL 回归，F-010 正式关闭。
- [x] 建立 F-011 本机候选：正式 Uvicorn 单 worker、启动前检查、数据目录实例锁、ready 等待和浏览器单次打开。

## 正在进行

- [ ] 与业务使用者核对旧功能的真实使用频率、责任角色和例外流程。
- [ ] 把首个切片之外“源码确认”的规则逐步细化为流程、状态机、字段字典和验收案例。
- [ ] 识别废弃工作簿、重复实现和仅用于临时辅助的工具。

## 下一步

1. 提交并推送 F-011 候选，确认 GitHub Actions 的 Linux 文件锁、重复实例、备份恢复启动和 PostgreSQL 回归全部绿色。
2. 执行 F-012 最终退出审查，在另一空目录复验安装、初始化、启动、备份和恢复。
3. Phase 1 关闭后进入 `SLICE-001` 可交互业务闭环。

## 当前阻塞与待决策

- F-011 首次 GitHub Quality 在恢复目录连续重启处失败；已对齐 Uvicorn 的安全地址复用语义，修复尚待远端验证。
- 最终桌面打包、快捷方式和内置 Python 运行时不属于 F-011，后续可在启动器基础上另行决定。
- 旧系统中哪些功能仍在实际使用，不能仅凭文件存在推断。
- 本机版何时升级 PostgreSQL、私有化授权方式等仍需 ADR。

## 最近验证

- 旧系统迁移前后均为 8,947 个文件、1,977,919,381 字节。
- 静态分析只读取工作簿和 VBA 源码；未运行宏，未修改旧文件。
- `.internal/legacy-pms/` 当前被 Git 忽略。
- 文档链接和 Markdown 格式应在每次文档批次完成后重新检查。
- 用户要求每个独立任务完成、验证并更新记录后创建一次 Git 提交。
- Phase 0 已于 2026-08-23 通过退出审查；F-001 已建立 Python 包与锁定环境，但尚未创建 Django 应用入口。
- F-001 使用 uv 0.12.5 和 CPython 3.14.7 解析、同步并锁定 56 个包；尚未创建 Django 应用入口。
- 锁定环境的 pip-audit 审计未发现已知漏洞；该结论只代表 2026-08-23 的扫描结果。
- F-002 的配置与 HTTP 测试、Django system check 和迁移漂移检查通过；未创建任何迁移。
- 修复 F-002 首次启动未创建 `PMS_DATA_DIR` 导致 SQLite 无法打开的问题；空库保持无用户表和业务表。
- F-003 本地质量链通过：Ruff、mypy、14 个 pytest、90.20% 分支覆盖率、Django checks 和依赖审计。
- F-004 已从空 SQLite 完成两次迁移验证：第一次成功，第二次无漂移；存在 `identity_user` 且不存在 `auth_user`。
- 用户确认 F-004 GitHub Actions 为绿色；PostgreSQL 18 首次迁移和身份集成测试通过，F-004 正式完成。
- F-005 在全新 SQLite 上首次迁移和重复迁移通过；25 项测试通过，覆盖率 96.71%。
- 用户确认 F-005 GitHub Quality 为绿色；PostgreSQL 18 迁移、成员约束和租户隔离测试通过，F-005 正式完成。
- F-006 本地质量链通过：43 个 pytest、98.36% 分支覆盖率、Ruff、mypy、Django checks 和迁移漂移检查均成功。
- F-006 在全新 SQLite 完成首次及重复迁移，5 张权限/审计表存在。
- 用户确认 F-006 GitHub Quality 为绿色；PostgreSQL 18 迁移、权限和审计集成测试通过，F-006 正式完成。
- F-007 本地质量链通过：60 个 pytest、98.80% 分支覆盖率、Ruff、mypy 和 Django checks 均成功。
- `live` 在禁止数据库访问时通过；`ready` 验证数据库和迁移，PostgreSQL 18 验证等待候选提交推送。
- GitHub Actions 运行 `32639337379` 为绿色；PostgreSQL 18 就绪探针和平台 HTTP 边界通过，F-007 正式完成。
- F-008 本地质量链通过：89 个 pytest、95.52% 分支覆盖率、Ruff、mypy、Django checks、迁移漂移和依赖审计均成功。
- 全新 SQLite 首次及重复迁移通过，附件表和私有目录存在；local `ready` 返回数据库、迁移和附件存储全部 `ok`。
- GitHub Actions 运行 `32641706801` 为绿色；PostgreSQL 18 附件迁移、约束和集成测试通过，F-008 正式完成。
- F-009 本地质量链通过：95 个 pytest、94.49% 分支覆盖率、Ruff、mypy、Django checks、迁移漂移、锁文件和依赖审计均成功。
- 系统临时目录中的全新 SQLite 完成首次及重复迁移、首次及无密码重复初始化；第二次新增计数全部为 0。
- 真实 Uvicorn 本机进程启动成功，`/health/ready` 返回 database、migrations 和 attachment_storage 全部 `ok`。
- GitHub Actions 运行 `32644256715` 为绿色；PostgreSQL 18 重复迁移、无密码重复初始化、Uvicorn 启动和就绪探针通过，F-009 正式完成。
- 从全新隔离克隆重新同步 56 个锁定包并通过完整质量链、迁移与初始化；真实 Uvicorn 根入口和 ready 均返回 200，克隆仓库保持干净。
- F-010 本地质量链通过：111 个 pytest 全部成功，分支覆盖率 87.83%；Ruff、mypy、Django checks、迁移漂移、锁文件和依赖审计均通过。
- F-010 在全新临时 SQLite 中完成迁移和初始化，成功创建、离线验证并恢复备份；恢复目录通过 Django check，真实 Uvicorn `/health/ready` 返回 200 且三项依赖全部 `ok`。
- F-010 反向测试覆盖源附件漂移、备份附件篡改或缺失、非空恢复目标、数据目录边界和重新计算清单摘要后的路径穿越。
- GitHub Actions 运行 `32648596251` 为绿色；F-010 的本机备份、离线验证、空目录恢复、恢复目录启动和 PostgreSQL 18 回归全部通过。
- F-011 本地质量链通过：127 个 pytest 全部成功，分支覆盖率 85.07%；Ruff、mypy、Django checks、迁移漂移、锁文件、文档链接和依赖审计均通过。
- F-011 在 Windows 使用真实恢复目录和端口 8891 启动，ready 返回 200；同一数据目录第二实例返回非零，首次停止后可重新启动。
- Windows Python 3.14 的 Uvicorn 在正常 shutdown 后会再次抛出 `KeyboardInterrupt`；启动器已将用户 `Ctrl+C` 收敛为无堆栈的正常停止并建立回归测试。
- F-011 GitHub Actions 运行 `32649827681` 的测试、格式、Lint 和类型检查通过，但本机烟雾在同一端口启动恢复目录实例时失败；Linux TIME_WAIT 下预检 socket 未设置 `SO_REUSEADDR`，与 Uvicorn 监听语义不一致。
- F-011 Linux 连续重启修复后本地质量链通过：128 个 pytest、85.08% 分支覆盖率、Ruff、mypy、Django checks、迁移漂移、锁文件与依赖审计均成功。

## 接续入口

继续工作时依次阅读：

1. 本页；
2. [项目蓝图](docs/product/blueprint.md)；
3. [路线图](docs/development/roadmap.md)；
4. [AI 与跨电脑交接说明](docs/development/handoff.md)；
5. [最近工作日志](docs/development/work-log.md)；
6. 当前任务对应的产品、架构或迁移文档。
