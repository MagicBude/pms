# 变更记录

本文件记录对新 PMS 仓库有意义的用户可见变更。格式参考 Keep a Changelog；版本号策略在首次可发布版本前确定。

## [Unreleased]

### Added

- 建立 Python 3.14、uv、`src/pms` 包结构、分组依赖和跨平台锁文件。
- 建立 F-001 开发环境搭建与依赖评审文档。
- 建立最小 Django ASGI、工程状态入口，以及 local、lan、cloud、test 配置档案。
- 建立配置快速失败、loopback 限制、云端安全选项和无数据库 HTTP 响应测试。
- 建立 Ruff、mypy、pytest、覆盖率、领域导入边界和 GitHub Actions 自动化质量门槛。
- 建立 UUIDv7 自有用户、用户名/密码认证、会话基础和项目第一次数据库迁移。
- 建立 tenant、membership、可信租户上下文和防止客户端伪造 tenant 的服务端解析边界。
- 建立稳定权限代码、默认角色矩阵、租户优先的对象范围授权和即时失效检查。
- 建立记录成功、拒绝与失败结果的追加式审计端口、持久化结构和敏感字段保护。
- 建立存活与就绪探针、request ID、安全 JSON 运行日志和稳定 HTTP 错误代码。
- 建立租户级附件元数据、随机存储键、本地原子写入、失败补偿和一致性对账。
- 建立显式、幂等的默认租户、初始管理员、权限角色矩阵初始化与空库烟雾检查。
- 建立本机 SQLite、附件和版本清单组成的一致性备份集、离线验证与空目录恢复命令。
- 建立本机 Uvicorn 正式启动器、启动前检查、跨平台数据目录实例锁、ready 等待和浏览器单次打开。
- 建立根级 `README.md`、`AGENTS.md`、项目状态页和文档分类。
- 建立代码、注释、命名、文档、测试、日志与错误、安全和 Git 规范。
- 建立项目蓝图以及本机、内网、云端三阶段演进方向。
- 建立路线图、追加式工作日志和跨电脑/跨 AI 交接机制。
- 建立旧系统功能清单、业务规则、新系统功能目录和迁移追踪矩阵初稿。
- 建立 P0 范围分层以及 `SLICE-001` 的流程、状态、权限和验收提案。
- 接受 P0 分层与 `SLICE-001`，建立业务术语、角色权限和核心状态机基线。
- 建立系统上下文、模块化单体边界以及技术栈、数据库/租户、附件存储和三阶段部署 ADR 提案。
- 建立 F-001 至 F-009 工程骨架与质量工具实施计划草案。
- 建立 45 条 `SLICE-001` Given/When/Then 正式验收案例和 Phase 0 退出审查。
- 建立租户级客户、单位、物料分类、物料和项目生命周期，业务写入统一执行权限、事务与审计。
- 建立 BOM 原文件安全留存、字段映射、逐行校验、人工匹配、重复确认、不可变发布、版本替代与差异查询。
- 建立投产数量快照、生产请购防重复、租户时区编号、原子提交、取消恢复和完整来源追踪。
- 建立项目、BOM、投产和生产请购的正式取消动作，统一保存取消原因、操作者和带时区时间，
  并在页面保留历史内容与取消原因。
- 建立本机登录、工作台、主数据、项目、BOM、投产和生产请购的服务端渲染页面，可从浏览器
  完成 `SLICE-001` 正常业务闭环。
- 建立版本化脱敏迁移包、可重复首切片导入和结构化新旧对账报告；未签收差异会让命令失败，
  虚构技术样例不能冒充业务确认。
- 建立完整业务链备份恢复验收和本机向前升级、空目录回退操作基线。
- 建立 Windows 双击首次安装与日常启动入口，自动定位仓库、使用固定本机数据目录，并通过
  PowerShell 隐藏读取和及时清除初始管理员密码。

### Changed

- 关闭项目后统一禁止新建 BOM、投产和生产请购，下游历史仍保持可追溯。
- 修复干净环境缺少本机数据目录时 Django 开发服务器无法启动的问题。
- 将原 Excel/VBA 项目整体迁入 `.internal/legacy-pms/`，从新系统仓库内容中隔离。
- 本机正式启动参数改由 local settings 统一提供，并增加受控端口与 ready 超时配置。
- 接受 `ADR-0001` 至 `ADR-0004`，将技术栈、数据库、多租户、附件和部署方案确立为实施约束。
- 扩展 Git 提交规范，增加 PMS scope、结构化正文、提交模板和验证记录要求。
- 接受 F-001 至 F-009 工程骨架与质量工具实施计划。
- 接受 `SLICE-001` 正式验收案例，Phase 0 退出审查通过并进入 Phase 1。
- 明确 Phase 2 只完成项目、BOM、投产和生产请购首切片，不能代表订单、价格、生产安排、库存、
  财务和报表等全部旧 PMS 功能已经实现。

### Security

- Git 忽略旧系统数据、凭证、附件、数据库及本地运行数据。
- 明确禁止运行来源不明的旧宏，并禁止把注册机私钥或真实敏感数据提交到仓库。
- 服务端权限默认拒绝且先验证 tenant；停用成员或解除角色后旧上下文不能继续授权。
- 审计摘要递归拒绝密码、令牌、会话、Cookie、Authorization、secret 和附件正文类字段。
- HTTP 异常响应和运行日志不输出异常原文、堆栈路径、SQL 或任意未允许字段。
- 附件原文件名不参与磁盘路径，跨租户 storage key、路径穿越和半成品读取均被拒绝。
- 本机恢复拒绝覆盖当前或非空数据目录，并校验备份文件集合、SQLite 完整性、迁移、记录数和全部附件摘要。
- 本机启动器拒绝 DEBUG、非 loopback、待迁移、未初始化、端口占用和同一数据目录重复实例。
- BOM 解析只接受受限 OOXML 文件，不执行宏或公式、不保留外部链接，并限制原文件及解压后大小。
- 本机工作台使用服务端 session、CSRF、即时 membership 复核和对象范围查询；附件下载继承
  业务对象权限，客户端不能提交 tenant 或目标状态。
- 工作台对权限与对象范围拒绝、业务冲突和受保护附件下载追加稳定审计结果，不保存异常原文、
  表单内容、附件正文或本机文件路径。
