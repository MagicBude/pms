# AGENTS.md

## 项目定位

本仓库是 PMS 新系统。旧 Excel/VBA 系统位于 `.internal/legacy-pms/`，只能作为需求、迁移和验收参考，不能继续向其中添加新系统功能。

项目同时承担生产系统建设和软件工程学习目的。实现必须正确、可维护、可测试，并让学习者能够理解关键设计原因。

## 开始工作前

1. 阅读 `PROJECT_STATUS.md`、`README.md` 和 `docs/product/blueprint.md`。
2. 阅读 `docs/development/handoff.md`、最近工作日志和任务相关分类文档。
3. 用 `git status` 检查已有修改，不覆盖来源不明或与当前任务无关的改动。
4. 修改代码前必须阅读 `docs/standard/code-standard.md` 和 `docs/standard/comment-standard.md`。
5. 涉及架构、数据模型、部署、安全或授权的重大决定，先新增或更新 ADR，再实现。
6. 不确定旧系统行为时，可以只读检查 `.internal/legacy-pms/`；不得运行来源不明的宏或可执行文件。

## 当前阶段

- 当前处于 Phase 0：蓝图和规范阶段。
- 未经用户明确要求，不创建具体框架工程，不安装生产依赖，不提前锁定技术栈。
- 文档中未决定的内容应标记为“待决策”，不要把推测写成已经确认的事实。

## 架构约束

- 使用模块化单体作为默认起点，不提前拆分微服务。
- 浏览器只通过应用服务访问数据，不能直接访问 SQLite 或共享数据库文件。
- 业务规则不能依赖具体 Web 框架、SQLite、PostgreSQL 或本地文件路径。
- 本机、内网和云端使用同一套业务代码，通过配置和基础设施适配器切换。
- 从第一天保留租户边界；业务数据必须能够明确归属 tenant。
- 所有租户级查询、唯一约束、缓存键、文件路径和后台任务都必须考虑 tenant。
- 金额使用十进制定点类型；时间使用带时区时间；禁止使用浮点数保存金额。
- 数据库结构变化必须通过迁移完成，禁止手工修改生产数据库结构。

## 旧系统保护规则

- `.internal/legacy-pms/` 默认为只读参考区。
- 未经用户明确授权，不删除、重命名、覆盖或批量格式化其中的任何文件。
- 不把旧系统数据库、凭证、发票、客户资料、员工资料或附件移回 Git 跟踪范围。
- 从旧系统提炼出的规则必须写入 `docs/` 或测试，不能只存在于聊天记录或个人记忆中。

## 编码与注释

- 遵守 `docs/standard/` 下的全部规范。
- 注释采用“教材级解释”：说明业务背景、设计原因、边界、单位、异常和取舍，同时避免逐行复述语法。
- 公共模块、公共类型、公共函数、核心领域规则、复杂算法、权限和租户隔离逻辑必须有文档注释。
- 代码和注释变更必须同步；过期注释视为缺陷。
- 标识符、接口字段和代码使用英文；面向团队的说明文档和业务解释优先使用中文。

## 测试与完成标准

- 修复缺陷时先建立能复现问题的测试，或在交付说明中解释为什么无法自动化复现。
- 新业务规则必须有单元测试；数据库和租户边界必须有集成测试。
- 多租户功能必须包含跨租户不可见、不可修改的反向测试。
- 完成任务前运行受影响范围的格式化、静态检查、类型检查和测试。
- 如果项目尚未建立对应命令，不得编造测试结果；应明确说明尚未配置。

## 安全与隐私

- 禁止提交密码、密钥、许可证私钥、真实客户数据和个人敏感信息。
- 日志不得记录密码、令牌、完整身份证号、银行卡号或附件原文。
- 授权签发私钥和“注册机”不得进入交付程序或本仓库。
- 所有外部输入均需验证；所有权限判断均在服务端执行。

## 文档维护

- 产品范围变更更新 `docs/product/`。
- 架构决策更新 `docs/architecture/` 或 ADR。
- 部署方式更新 `docs/deployment/`。
- 开发流程更新 `docs/development/`。
- 工程规则更新 `docs/standard/`。
- 安全和授权更新 `docs/security/`。
- 旧系统迁移与验收映射更新 `docs/migration/`。
- 每个工作批次结束前更新 `PROJECT_STATUS.md` 和 `docs/development/work-log.md`。
- 有用户可见变化时更新 `CHANGELOG.md`；阶段或优先级变化时更新路线图。
- 旧系统结论必须标记为“源码确认”“合理推断”或“优化建议”，并引用稳定功能/规则编号。

## 变更纪律

- 保持变更范围聚焦，不顺带重构无关模块。
- 不覆盖用户已有但与当前任务无关的改动。
- 新依赖必须说明用途、替代方案、维护状态和安全影响。
- 重大决定使用 ADR 记录背景、选择、备选方案和后果。
- 每个独立任务完成并通过相应检查后必须创建一次 Git 提交，形成可追溯、可回档的基线。
- 提交遵循 Conventional Commits：类型前缀使用英文，冒号后的说明优先使用中文。
- 不把多个尚未验证或互不相关的任务捆绑进同一提交；提交前更新状态页、工作日志和必要文档。
