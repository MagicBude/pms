# Git 规范

状态：已接受

## 1. 仓库边界

只提交新系统源码、测试、迁移、非敏感配置样例和文档。

禁止提交：

- `.internal/legacy-pms/`；
- 真实客户、供应商、员工和财务数据；
- 发票、凭证、合同、图纸和生产附件；
- 数据库文件、备份和运行日志；
- 密码、令牌、私钥和生产连接字符串；
- 构建产物、依赖目录和本机 IDE 文件。

## 2. 分支

- 默认分支保持可构建、可测试。
- 功能分支短生命周期并聚焦一个目标。
- Codex 创建分支时默认使用 `codex/` 前缀。
- 不在功能分支混入无关格式化、批量重命名或数据文件。

## 3. 提交

一个提交表达一个逻辑变更，并能够独立评审。

推荐提交类型：

```text
docs: define project blueprint
feat: add purchase order confirmation
fix: prevent cross-tenant invoice lookup
test: cover payment rounding boundary
refactor: isolate attachment storage adapter
chore: configure static analysis
```

提交说明应解释原因和影响，不只重复文件名。生成代码和迁移应与产生它们的逻辑变更一起提交。

## 4. 变更前检查

- 确认工作树已有改动的归属，不能覆盖用户无关修改。
- 检查 `.gitignore` 是否覆盖新生成数据。
- 涉及重大决策时先更新 ADR 或产品文档。
- 涉及数据库时确认迁移和回滚策略。

## 5. 提交前检查

- 查看完整 diff 和未跟踪文件。
- 运行格式化、Lint、类型检查和受影响测试。
- 扫描秘密和真实数据。
- 确认注释、文档、迁移和测试同步。
- 不使用 `--no-verify` 绕过正常质量门槛，除非有明确原因并记录。

## 6. 历史安全

- 不使用强制推送覆盖他人已共享历史，除非团队明确协调。
- 不使用破坏性重置处理不属于自己的改动。
- 如果秘密或敏感数据已经进入历史，删除工作树文件不够，必须执行密钥轮换和受控历史清理。

## 7. 评审

评审重点依次是：业务正确性、数据和租户安全、可恢复性、测试证据、可维护性和风格。

格式问题应尽量由自动化工具处理，人工评审集中于工具无法判断的设计和业务风险。

