# 工作日志

状态：追加式维护

本文件记录执行事实和验证结果，不替代 Git 历史、变更记录或产品决策。新记录放在最上方；不要改写历史结论，如需纠正应新增一条说明。

## 2026-08-23：F-005 PostgreSQL 18 CI 验证通过

### 完成

- 用户确认包含 tenancy 迁移和反向隔离测试的 GitHub `Quality` 工作流为绿色。
- PostgreSQL 18 成功验证 tenant、membership、唯一约束和可信上下文查询。
- F-005 达到 SQLite 与 PostgreSQL 双数据库完成标准。

### 下一动作

- 创建 F-005 验证闭环提交。
- 进入 F-006 权限策略、角色授权和追加式审计边界。

## 2026-08-23：建立 F-005 租户与成员候选基线

### 完成

- 建立 UUIDv7 `Tenant` 和 `Membership` ORM 映射及首次租户迁移。
- 数据库限制同一用户在同一租户只有一条 membership，同时允许用户加入多个租户。
- 建立纯 Python `TenantContext`、应用层查询端口和 Django ORM 适配器。
- 解析入口只接受会话用户和 membership，不接受客户端声明的 tenant ID。
- 覆盖其他用户 membership、停用 membership、停用 tenant、重复关系和多租户成员反向测试。
- CI 的 PostgreSQL 18 步骤扩展为执行 identity 与 tenancy 集成测试。

### 判断

- tenant 与 membership 只承担企业归属，不提前混入 F-006 角色和权限。
- 查询失败统一为成员关系不可用，避免泄露其他用户的 membership 是否存在。
- 默认本机 tenant 和管理员初始化留在 F-009，迁移不产生环境相关数据。

### 验证

- Ruff、严格 mypy、25 个 pytest 和 96.71% 分支覆盖率通过。
- 全新 SQLite 完整迁移成功，第二次迁移无操作。
- SQLite 创建 `tenancy_tenant` 和 `tenancy_membership`，唯一约束测试通过。
- PostgreSQL 18 迁移与约束验证等待候选提交推送后的 GitHub Quality 工作流。

### 下一动作

- 创建 F-005 候选提交并由用户推送。
- CI 绿色后关闭 F-005，再进入 F-006 权限与审计。

## 2026-08-23：F-004 PostgreSQL 18 CI 验证通过

### 完成

- 用户将候选提交推送到 GitHub，并确认 `Quality` 工作流为绿色。
- PostgreSQL 18 临时服务成功执行首次迁移和 identity 集成测试。
- F-004 同时满足 SQLite 本地验证与 PostgreSQL 18 CI 验证，转为正式完成。

### 判断

- PostgreSQL 只存在于自动化测试环境，不改变本机版继续使用 SQLite 的部署决策。
- GitHub Pages 与 CI 无关，当前项目不启用 Pages 静态站点发布。

### 下一动作

- 创建 F-004 验证闭环提交。
- 进入 F-005 tenant、membership 与可信租户上下文。

## 2026-08-23：建立 F-004 自有用户候选基线

### 完成

- 按已接受 P0 范围使用全局唯一用户名和密码登录，邮箱保持可选且不作为登录标识。
- 建立基于 `AbstractUser` 的 `identity.User`，主键由 Python 3.14 生成 UUIDv7。
- 启用 auth、contenttypes、sessions 和认证中间件，并设置 `AUTH_USER_MODEL`。
- 创建带业务意图、回滚边界和无旧数据回填说明的第一次自有迁移。
- 建立创建、密码哈希、认证、停用、会话退出和用户名唯一性测试。
- 在 CI 增加 PostgreSQL 18 首次迁移和身份集成测试步骤。

### 判断

- 全局用户不直接保存 tenant 或租户角色；F-005 使用 membership 表达多公司归属。
- `is_staff`/`is_superuser` 只是受控运维能力，不能等同租户管理员或绕过 tenant。
- 当前不提供登录 UI、Django Admin、找回密码、登录审计和速率限制，不提前扩大 F-004。

### 验证

- Ruff、mypy 和 19 个 pytest 通过，分支覆盖率保持高于 80%。
- 从全新 SQLite 执行迁移成功，第二次执行显示无迁移可应用。
- SQLite 存在 `identity_user`，不存在默认 `auth_user`。
- 本机没有 Docker/PostgreSQL，PostgreSQL 18 迁移与身份测试已写入 CI，等待推送后验证。

### 下一动作

- 创建 F-004 候选提交并由用户推送。
- GitHub Actions PostgreSQL 18 验证通过后关闭 F-004；失败则在本批次修复。

## 2026-08-23：完成 F-003 自动化质量门槛

### 完成

- 在 `pyproject.toml` 集中配置 Ruff、mypy、django-stubs、pytest、测试标记和分支覆盖率。
- 建立领域层导入边界测试，提前拒绝 Django、数据库驱动、ASGI 服务器和 PMS 基础设施依赖。
- 建立 GitHub Actions，对 Pull Request 和 `main` 推送执行锁定同步、静态检查、测试、Django checks 和依赖审计。
- CI 声明 PostgreSQL 18 服务，为 F-004 首批迁移后的双数据库测试预留一致环境。

### 判断

- 未采用 Ruff `ALL`：其中英文标点、版权头和 pytest 风格规则与项目中文教材级注释及现有 Django 测试风格冲突；改为显式高价值规则集合。
- 覆盖率门槛设为 80%，当前为 90.20%；隔离子进程中的部署档案使用行为测试，不把无法准确归集的数据混入覆盖率。
- CI 第三方 `setup-uv` 固定完整提交 SHA，并固定 uv 0.12.5；仓库检出使用 GitHub 官方当前主版本。

### 验证

- `ruff format --check .` 和 `ruff check .` 通过。
- `mypy src tests` 严格模式检查 14 个源码文件通过。
- pytest 14 项通过，分支覆盖率 90.20%，高于 80% 门槛。
- Django system check、迁移漂移检查、锁文件检查和依赖安全审计通过。

### 下一动作

- 提交 F-003 独立回档点。
- 进入 F-004，在第一次迁移中建立自有用户模型与认证基础。

## 2026-08-23：修复本机首次启动数据目录缺失

### 完成

- 根据用户提供的完整堆栈复现 `sqlite3.OperationalError: unable to open database file`。
- 确认根因是 `local` 档案指向尚不存在的 `data/` 父目录。
- 在本机配置加载时创建应用私有数据目录，并将目录错误转换成不泄露绝对路径的配置错误。
- 新增干净临时目录下连接 SQLite 且数据库结构保持为空的回归测试。

### 验证

- 在不存在的嵌套 `PMS_DATA_DIR` 下执行 Django 启动迁移检查成功。
- SQLite `sqlite_master` 中没有用户表、迁移表或业务表。
- 本机真实 HTTP 启动仍返回 200。

### 下一动作

- 创建独立缺陷修复提交后继续 F-003。

## 2026-08-23：完成 F-002 Django ASGI 与配置档案

### 完成

- 建立 `manage.py`、ASGI 入口、平台 URL 和不依赖数据库的最小 HTTP 响应。
- 建立 `base`、`local`、`lan`、`cloud`、`test` 配置档案以及集中环境变量解析。
- 本机档案强制 loopback；内网和云端缺少密钥、主机或 PostgreSQL 参数时快速失败。
- 补充无秘密 `.env.example`、部署配置说明和 7 个配置/HTTP 回归测试。

### 判断

- F-002 根页面只用于证明浏览器链路，不提前形成业务 UI。
- Uvicorn 的监听参数位于进程边界，因此 settings 校验 `PMS_BIND_HOST`，启动命令仍必须传相同地址；后续启动器统一两者。
- F-004 前不启用 auth、sessions 和 admin，避免默认用户表进入迁移历史。

### 验证

- `python manage.py check` 通过，无 system check 问题。
- `python -m unittest discover -v` 的 7 个测试通过。
- Uvicorn 实际监听 `127.0.0.1` 后，HTTP 请求返回 200、预期中文提示及安全响应头。
- `makemigrations --check --dry-run` 输出 `No changes detected`。
- ASGI 应用可导入；检查后没有生成 SQLite 文件或迁移。

### 下一动作

- 提交 F-002 独立回档点。
- 进入 F-003，建立 Ruff、mypy、pytest、覆盖率、导入边界和 CI。

## 2026-08-23：完成 F-001 Python 工程与依赖锁

### 完成

- 确认 Phase 0 基线与 `origin/main` 同步后进入 F-001。
- 从 Astral 固定版本官方地址安装 uv 0.12.5，并由 uv 安装 CPython 3.14.7。
- 建立 `pyproject.toml`、`.python-version`、`src/pms` 和 uv_build 纯 Python 包边界。
- 分组声明运行、服务器、开发、测试和安全依赖，生成并提交跨平台 `uv.lock`。
- 编写开发环境搭建和直接依赖评审，记录用途、替代、维护、安全与许可证。

### 判断

- Django 下限使用 5.2.8，因为该补丁起正式支持 Python 3.14；锁文件实际解析为 5.2.17。
- 项目元数据暂用 `0.0.0` 表示未发布工程，不提前决定正式版本策略。
- uv_build 适合当前纯 Python `src/pms` 结构；需要扩展模块或构建脚本时再评估 Hatchling。
- `pip-audit --locked` 不识别 uv.lock，因此对严格同步后的虚拟环境执行安全审计并记录限制。

### 验证

- `uv lock --check` 通过，解析并安装 56 个包。
- 从空 `.venv` 执行 `uv sync --locked --all-groups` 成功；离线重复同步无漂移。
- CPython 3.14.7 下成功导入 `pms`、Django、openpyxl、Uvicorn 和 psycopg。
- uv_build 成功生成源码包和 wheel；`dist/` 保持 Git 忽略。
- `pip-audit --local --skip-editable` 未发现已知漏洞。
- 未创建 Django settings、ASGI、数据库迁移或业务模块，未读取旧系统文件。

### 下一动作

- 提交 F-001 独立回档点。
- 进入 F-002，建立不启用认证和数据库迁移的最小 ASGI 与配置档案。

## 2026-08-23：接受 SLICE-001 验收案例并退出 Phase 0

### 完成

- 用户接受 45 条 `SLICE-001` Given/When/Then 验收案例。
- 将验收案例标记为“已接受”，Phase 0 退出审查标记为“已通过”。
- 项目当前阶段切换到 Phase 1，下一实施批次固定为 F-001。

### 判断

- 首个切片的范围、规则、数据、权限、验收、架构和工程计划已经可实施。
- 首个切片之外的旧功能访谈继续进行，但不阻塞通用工程基础。
- `AC-S001-043` 脱敏旧案例仍阻塞最终业务验收，不阻塞 F-001 至 F-009。

### 验证

- 验收案例、退出审查、项目状态和路线图的阶段状态一致。
- 当前工作树在变更前干净，旧系统仍受保护且未被读取或修改。
- 本批次仍未创建代码工程或安装依赖。

### 下一动作

- 创建 Phase 0 基线提交，不自动创建 Git tag。
- 同步基线后执行 F-001 Python 工程和依赖锁。

## 2026-08-23：建立 SLICE-001 正式验收案例并审查 Phase 0

### 完成

- 将 14 条候选验收摘要细化为 45 条稳定 `AC-S001-xxx` Given/When/Then 案例。
- 为每条案例标注功能、规则或决策来源以及计划测试层级。
- 覆盖初始化、身份、项目、BOM 文件与版本、投产、请购、并发、事务、权限、租户、附件、审计、恢复和旧系统对账。
- 建立原 `AT-S001` 至 `AT-S014` 与正式案例的追踪映射。
- 更新首个切片迁移追踪状态，并执行 Phase 0 退出条件审查。

### 判断

- 除旧系统结果对账 `AC-S001-043` 需要业务确认的脱敏样本外，其余案例原则上可以自动化。
- 脱敏旧样本不阻塞 Phase 1 工程基础，但阻塞 `SLICE-001` 最终业务验收。
- 当前 Phase 0 不能提前宣布完成；正式验收案例仍需用户接受。

### 验证

- 对照 `D-S001` 至 `D-S012`、角色权限矩阵、四个状态机、业务规则和附件 ADR 检查案例覆盖。
- 检查 45 个案例 ID 连续且唯一，每条均包含 Given、When、Then。
- 本批次只修改文档，未创建工程代码、安装依赖或执行旧宏。

### 下一动作

- 由用户评审并接受 `SLICE-001` 正式验收案例。
- 接受后更新退出审查和项目阶段，创建 Phase 0 基线提交。

## 2026-08-23：接受工程骨架实施计划

### 完成

- 用户确认继续下一步，将工程骨架与质量工具实施计划从“评审中”转为“已接受”。
- 同步项目状态、路线图和变更记录，移除计划未接受这一阻塞项。

### 判断

- 接受计划确认 F-001 至 F-009 的实施顺序、边界和验收门槛。
- 当前仍处于 Phase 0；必须先完成 `SLICE-001` 验收案例和退出审查，不能直接创建工程代码。

### 验证

- 计划正文、项目状态和路线图的接受状态一致。
- 本批次只改变计划状态和治理记录，未创建代码或安装依赖。

### 下一动作

- 编写 `SLICE-001` Given/When/Then 验收案例。
- 根据全部 Phase 0 交付物执行退出审查。

## 2026-08-23：形成工程骨架实施计划

### 完成

- 依据已接受 ADR 编写工程骨架与质量工具实施计划。
- 定义目标目录、配置档案、依赖分组、质量命令、测试矩阵和 Phase 1 完成标准。
- 将实施拆分为 F-001 至 F-009，每批都有建议提交、主要内容和可验证验收条件。
- 明确自有用户模型、双数据库验证、租户隔离、权限审计、健康检查和附件端口的实施顺序。

### 判断

- 不提前生成全部业务模块，只创建当前批次真正需要的包。
- 先锁定工程、配置和质量门槛，再建立 identity、tenancy、authorization 和 audit，最后验证可重复初始化。
- 自有用户模型必须在其他业务迁移引用用户前建立；PostgreSQL 验证不能推迟到内网部署阶段。
- 计划中的命令是目标契约，当前尚未配置，不能写成已经通过。

### 验证

- 对照四份 ADR、模块边界以及代码、注释、命名、测试、日志和安全规范检查计划。
- 检查每个实施批次都有输入范围、验收证据和独立提交建议。
- 本批次未创建 Django 工程、未安装依赖、未修改旧系统文件。

### 下一动作

- 由用户评审并接受工程骨架实施计划。
- 同时补齐 `SLICE-001` Given/When/Then 验收案例，满足 Phase 0 退出条件。

## 2026-08-23：接受关键架构并升级提交规范

### 完成

- 用户确认继续下一步，将系统上下文、模块边界以及 `ADR-0001` 至 `ADR-0004` 从草案或提议转为“已接受”。
- 同步架构、部署、蓝图、路线图和项目状态中的决策状态与待决策事项。
- 参考用户提供的《Git 提交规范》，为 PMS 增加 `type(scope): 中文主题`、项目 scope 清单和结构化正文要求。
- 新增 `.gitmessage` 模板，并同步 Agent 与跨电脑交接规则。

### 判断

- 参考文档作为规范来源而不是仓库指令，采用其原子性、可追溯性和完整提交正文原则。
- 嵌入式模块 scope、固定 `develop` 分支和 Node commitlint 不适合当前 Phase 0，因此没有机械复制。
- 非简单提交必须让接手者仅查看 Git 历史就能理解动机、主要变更、验证和影响。

### 验证

- 检查四份 ADR 正文与索引状态一致。
- 检查蓝图已决策事项与 ADR 一致，未把启动器、打包和存储供应商等待决项写成事实。
- 本批次只修改治理和架构文档，未创建工程代码或安装依赖。

### 下一动作

- 根据已接受 ADR 单独编写工程骨架与质量工具实施计划。
- 后续所有非简单提交使用带 scope、正文和验证信息的完整提交消息。

## 2026-08-23：形成关键架构决策提案

### 完成

- 建立系统上下文，明确浏览器、应用服务、数据库、附件存储和外部系统的边界。
- 建立模块化单体边界，定义模块职责、依赖方向、数据所有权、事务和跨模块协作原则。
- 形成技术栈、数据库与多租户、附件存储、三阶段部署四份 ADR 提案。
- 查证 Django、Python、SQLite、PostgreSQL、uv、Ruff、HTMX、openpyxl 和 Uvicorn 的官方资料，将版本与能力依据记录在 ADR 中。
- 更新架构和部署导航，使后续接手者能区分“提议”与“已接受”决策。

### 判断

- 推荐 Python 3.14、Django 5.2 LTS、服务端模板和 HTMX，以一套模块化单体支持本机、内网和云端。
- SQLite 仅用于本机单用户；进入内网多人阶段即切换 PostgreSQL 18，禁止共享数据库文件。
- 采用共享数据库、共享 Schema 和显式 `tenant_id`，P0 不依赖 PostgreSQL RLS，但所有租户查询和约束都必须显式隔离。
- 附件二进制与业务元数据分离，通过存储适配器从本地文件系统演进到对象存储。
- 上述内容均为提议，用户接受前不得据此锁定工程实现。

### 验证

- 本批次只修改文档，未创建框架工程，未安装依赖，未修改旧系统文件。
- 对关键版本、数据库适用边界、文件存储与部署能力使用官方资料进行交叉核对。
- 完成前检查仓库内 Markdown 链接、标题锚点、尾随空白和常见敏感信息模式。

### 下一动作

- 由用户评审 `ADR-0001` 至 `ADR-0004` 的提议内容。
- ADR 接受后，单独编写工程骨架实施计划并提交。

## 2026-08-23：P0 收敛与首个业务切片确认

### 完成

- 确认首次提交 `e0d3ef1` 已由用户手动推送，本地 `main` 与 `origin/main` 同步。
- 将宽泛 P0 划分为 P0-A 平台基础、P0-B 首个切片、P0-C 采购履约扩展。
- 为推荐的 `SLICE-001` 编写目标、范围、角色、流程、状态、核心字段、规则、权限和 14 个候选验收案例。
- 用户接受 P0 分层以及 `D-S001` 至 `D-S012` 的全部推荐值。
- 将 `SLICE-001` 和 P0 范围文档标记为已接受。
- 建立首个切片业务术语表、稳定权限代码与默认矩阵、四个核心状态机。

### 判断

- 一次实现所有 P0 会把供应商、采购订单和入库过早耦合到首版，难以独立验收。
- `项目 → BOM → 投产 → 生产请购` 能先验证最重要的数据边界、版本、数量、防重复、编号和审计。
- 本批次形成首个可实施的产品基线，后续架构 ADR 和测试必须引用这些稳定术语、权限和状态。

### 验证

- 开始工作时 Git 工作区干净，本地与远程位于同一提交。
- 本次仅修改项目文档，未读取或修改旧系统文件。
- 用户要求以后每个独立任务完成后创建 Git 提交，以便追溯和回档。

### 下一动作

- 创建技术栈、数据库/租户、附件存储和部署 ADR。
- ADR 通过后建立工程骨架和自动化质量工具。

## 2026-08-22：建立连续工作机制与第一轮业务提取

### 完成

- 新增项目状态、路线图、工作日志、变更记录和 AI 交接说明。
- 对 55 个关键工作簿进行只读静态盘点。
- 39 个工作簿检测到 VBA，共提取 317 个模块和 1,309 个过程，提取错误为 0。
- 整理菜单入口、工作簿职责、关键字段、编号、去重、提交、财务和库存规则。
- 将结果拆分为旧功能清单、业务规则、新功能目录和迁移追踪矩阵。

### 判断

- 旧文件名、工作表和文件夹同时承担了数据库、界面、状态和附件索引职责；新系统必须把这些职责拆开。
- 多个工作簿重复实现筛选、拼音输入、格式化、文件查找和数组合并；新系统应提供共享组件和应用服务。
- 工作簿存在不代表功能仍在使用，因此静态结果只能作为需求候选，必须再经过业务确认。

### 验证

- 未运行任何旧宏或可执行文件。
- 未修改任何 `.internal/legacy-pms/` 文件。
- 静态提取的临时 VBA 文本不纳入仓库。

### 下一动作

- 评审 P0 功能并选择首个业务切片。
- 以真实案例确认关键状态、异常和权限。

## 2026-08-21：隔离旧系统并建立 Phase 0 文档

### 完成

- 将原项目 8,947 个文件整体迁入 `.internal/legacy-pms/`。
- 迁移前后总大小均为 1,977,919,381 字节。
- 创建分类文档目录、根说明、Agent 规则和 `.gitignore`。
- 编写工程规范和项目蓝图。

### 验证

- 旧系统迁移前后文件数和总字节数一致。
- `.internal/legacy-pms/` 被 Git 忽略。
